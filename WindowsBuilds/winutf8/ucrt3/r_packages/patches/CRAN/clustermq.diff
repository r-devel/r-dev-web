diff -ruN orig/clustermq/src/Makevars.win patched/clustermq/src/Makevars.win
--- orig/clustermq/src/Makevars.win	2020-06-27 04:07:11.000000000 -0700
+++ patched/clustermq/src/Makevars.win	2021-01-11 09:31:35.029009000 -0800
@@ -1,19 +1,12 @@
 CXX_STD = CXX11
-GCC_VERSION = ${subst gcc,,${COMPILED_BY}}
-RWINLIB = ../windows/zeromq-4.2.1
 
 PKG_CPPFLAGS = -DSTRICT_R_HEADERS -DZMQ_STATIC \
-	-I${RWINLIB}/include -I../inst
+	-I../inst
 
 PKG_LIBS = \
-	-L${RWINLIB}/lib${GCC_VERSION}${R_ARCH} \
-	-L${RWINLIB}/lib${R_ARCH} \
-	-lzmq -lsodium -liphlpapi -lws2_32
+	-lzmq -lsodium -liphlpapi -lws2_32 -fstack-protector
 
-all: clean winlibs
+all: clean 
 
 clean:
 	rm -f $(OBJECTS) $(SHLIB)
-
-winlibs:
-	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" "../tools/winlibs.R"
diff -ruN orig/clustermq/tools/winlibs.R patched/clustermq/tools/winlibs.R
--- orig/clustermq/tools/winlibs.R	2020-06-27 04:07:11.000000000 -0700
+++ patched/clustermq/tools/winlibs.R	1969-12-31 16:00:00.000000000 -0800
@@ -1,7 +0,0 @@
-# Build against precompiled zeromq libs.
-if(!file.exists("../windows/zeromq-4.2.1/include/zmq.h")){
-  download.file("https://github.com/rwinlib/zeromq/archive/v4.2.1.zip", "lib.zip", quiet = TRUE)
-  dir.create("../windows", showWarnings = FALSE)
-  unzip("lib.zip", exdir = "../windows")
-  unlink("lib.zip")
-}
