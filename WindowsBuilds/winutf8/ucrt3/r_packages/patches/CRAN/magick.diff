diff -ruN orig/magick/src/Makevars.win patched/magick/src/Makevars.win
--- orig/magick/src/Makevars.win	2020-10-11 12:58:00.000000000 -0700
+++ patched/magick/src/Makevars.win	2021-01-12 02:44:40.778491200 -0800
@@ -1,37 +1,15 @@
-# ImageMagick build target
-IM=6
-VERSION=6.9.11-34
-IMPATH = ../windows/imagemagick${IM}-${VERSION}
-LIBBUILD = ${subst gcc ,-,${R_COMPILED_BY}}${R_ARCH}
-
-PKG_CPPFLAGS = -I$(IMPATH)/include${LIBBUILD} \
-	-I$(IMPATH)/include-config${R_ARCH} \
-	-I$(IMPATH)/include/ImageMagick-${IM}
 
+PKG_CPPFLAGS = -I${LOCAL_SOFT}/include/ImageMagick-7
 PKG_CXXFLAGS = -DMAGICKCORE_HDRI_ENABLE=0 -DMAGICKCORE_QUANTUM_DEPTH=16 -D_LIB
 
 CXX_STD = CXX11
 
-PKG_LIBS = \
-	-L$(IMPATH)/lib${LIBBUILD} \
-	-L$(IMPATH)/lib${R_ARCH} \
-	-lMagick++-${IM}.Q16 -lMagickWand-${IM}.Q16 -lMagickCore-${IM}.Q16 \
-	-lfftw3 -llcms2 -ljpeg -ltiff -llzma -lbz2 \
-	-lrsvg-2 -lxml2 \
-	-lpangocairo-1.0 -lpango-1.0 -lpangowin32-1.0 \
-	-lcairo -lfreetype -lpixman-1 -lgdk_pixbuf-2.0 \
-	-lcairo-gobject -lgio-2.0 -lgobject-2.0 -lgmodule-2.0 -lglib-2.0 \
-	-lffi -lpcre -lpng16 -lintl -lz -liconv -lfribidi \
-	-lopenjp2 -lwebpmux -lwebp -lwebpdecoder -lwebpdemux \
-	-lusp10 -liphlpapi -lgdiplus -lgdi32 -lole32 -ldnsapi -lws2_32 -luserenv
+PKG_LIBS = -fopenmp \
+-lMagick++-7.Q16HDRI -lMagickWand-7.Q16HDRI -lMagickCore-7.Q16HDRI -llqr-1 -lraw_r -lraw -lfftw3 -lwebpmux -lIlmImf -ljasper -llcms2 -lHalf -lIlmThread -lIex -lfontconfig -lxml2 -lopenjp2 -lexpat -lfreetype_too -lharfbuzz_too -lfreetype -lpng16 -lpng -lharfbuzz -lglib-2.0 -lbz2 -lintl -liconv -lwsock32 -lws2_32 -ltiff -llzma -ljpeg -lz -lcfitsio -lzstd -lwebpdecoder -lwebp -lsbml-static -lmincore -lgdi32
 
-all: clean winlibs
+all: clean 
 
 clean:
 	rm -f $(OBJECTS) $(SHLIB)
 
-winlibs:
-	@echo "Linking to imagemagagick-$(VERSION)"
-	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" "../tools/winlibs.R" ${VERSION}
-
-.PHONY: all winlibs clean
+.PHONY: all clean
