diff -Nru orig/flowCore/src/Makevars.ucrt patched/flowCore/src/Makevars.ucrt
--- orig/flowCore/src/Makevars.ucrt	1970-01-01 01:00:00.000000000 +0100
+++ patched/flowCore/src/Makevars.ucrt	2021-08-14 22:02:35.308599600 +0200
@@ -0,0 +1,12 @@
+CXX_STD = CXX11
+RHDF5_LIBS= $(shell "${R_HOME}/bin/Rscript" -e "Rhdf5lib::pkgconfig('PKG_CXX_LIBS')")
+PKG_LIBS += `"${R_HOME}/bin/Rscript" -e "cytolib:::cytolib_LdFlags();cat(' ');RProtoBufLib:::LdFlags()"` ${RHDF5_LIBS} $(LAPACK_LIBS) $(BLAS_LIBS) -lstdc++fs -lbcrypt
+
+.PHONY: all clean 
+
+all: $(SHLIB) 
+	if [ `uname -s` = 'Darwin' ]; then install_name_tool -change /usr/local/clang4/lib/libc++.1.dylib /usr/lib/libc++.1.dylib $(SHLIB); fi
+
+
+clean:
+	rm -f $(OBJECTS) $(SHLIB)
