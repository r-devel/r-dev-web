diff -r -u src/gnuwin32/front-ends/Renviron.c src/gnuwin32/front-ends/Renviron.c
--- src/gnuwin32/front-ends/Renviron.c	2021-09-13 11:54:23.326632500 +0200
+++ src/gnuwin32/front-ends/Renviron.c	2021-10-27 00:32:38.293869400 +0200
@@ -1,6 +1,6 @@
 /*
  *   R : A Computer Language for Statistical Data Analysis
- *   Copyright (C) 1997-2008   The R Core Team
+ *   Copyright (C) 1997-2021   The R Core Team
  *
  *  This program is free software; you can redistribute it and/or modify
  *  it under the terms of the GNU General Public License as published by
@@ -81,7 +81,7 @@
 static char *findterm(char *s)
 {
     char *p, *q, *r, *r2, *ss=s;
-    static char ans[1000];
+    static char ans[10000];
     int nans;
 
     if(!strlen(s)) return "";
@@ -99,11 +99,11 @@
 	strncpy(r, p, q - p + 1);
 	r[q - p + 1] = '\0';
 	r2 = subterm(r);
-	if(strlen(ans) + strlen(r2) < 1000) strcat(ans, r2); else return ss;
+	if(strlen(ans) + strlen(r2) < 10000) strcat(ans, r2); else return ss;
 	/* now repeat on the tail */
 	s = q+1;
     }
-    if(strlen(ans) + strlen(s) < 1000) strcat(ans, s); else return ss;
+    if(strlen(ans) + strlen(s) < 10000) strcat(ans, s); else return ss;
     return ans;
 }
 
@@ -147,8 +147,8 @@
 }
 
 
-#define BUF_SIZE 255
-#define MSG_SIZE 2000
+#define BUF_SIZE 1024 
+#define MSG_SIZE 8000
 int process_Renviron(const char *filename)
 {
     FILE *fp;
