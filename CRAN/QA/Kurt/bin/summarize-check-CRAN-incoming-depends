#! /usr/bin/Rscript
### -*- R -*-

dir <- normalizePath(file.path("~", "tmp", "CRAN"))

args <- commandArgs(trailingOnly = TRUE)
if(length(args)) {
    dir <- args[1L]
}

tools::summarize_check_packages_in_dir_depends(dir)
